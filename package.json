{
  "name": "root",
  "private": true,
  "scripts": {
    "dev:one": "lerna run --scope hostingone dev --port 3000",
    "build:one": "lerna run --scope hostingone build",
    "generate:one": "lerna run --scope hostingone generate",
    "dev:two": "lerna run --scope hostintwo dev --port 3001",
    "build:two": "lerna run --scope hostintwo build",
    "generate:two": "lerna run --scope hostingtwo generate"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "firebase": "^6.5.0",
    "firebase-tools": "^7.3.0"
  }
}
